{
  "name": "@lumera/precompile-examples",
  "version": "0.1.0",
  "description": "Solidity interfaces and example contracts for Lumera's custom EVM precompiles",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "deploy:devnet": "hardhat run scripts/deploy.ts --network devnet",
    "interact:devnet": "hardhat run scripts/interact.ts --network devnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "hardhat": "^3.9.0",
    "ethers": "^6.14.0",
    "chai": "^5.2.0",
    "mocha": "^11.0.0",
    "@types/chai": "^5.2.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  },
  "overrides": {
    "lodash-es": "^4.18.1",
    "serialize-javascript": "^7.0.6",
    "diff": "^8.0.4"
  }
}
